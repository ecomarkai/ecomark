# Connection manager UI specification

Screens: list and health; add preset/custom endpoint; OAuth or secret entry; compatibility test;
tool discovery/review; agent bindings; cost limits; audit/run history; disconnect.
Show unsupported transport/auth explicitly. List tool side effects and data sent externally.
Never display stored secrets. Tenant custom endpoints require the gateway security review flow.
