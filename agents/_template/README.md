# Agent template

Copy this directory only through the platform scaffolding workflow. Replace every example value.
A production agent is a governed decision component, not a prompt. It must declare inputs,
outputs, capabilities, policies, approvals, metrics, evaluations and ownership.
