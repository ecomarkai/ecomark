# Security

Report vulnerabilities privately to security@ecomark.ai. Do not include production data.
All tenant access must be scoped by tenant_id, all secrets must use the secrets manager, and all
agent actions must pass authorization, policy, approval and audit controls.
