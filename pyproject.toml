[project]
name = "ecomark-platform"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["agents", "connectors", "services", "tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
