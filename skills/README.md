# Ecomark Skills

A Skill is a versioned package of instructions, capability requirements, references, optional scripts,
assets and evaluations that an Agent can load for a bounded task.

Agents are long-lived roles. Skills are installable competencies. A skill never receives vendor
credentials and cannot bypass policy, approval, tenant isolation or audit.
