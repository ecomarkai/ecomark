# Ecomark MCP platform

MCP is an external interoperability boundary. Agents use internal capability contracts; the MCP
gateway safely exposes selected read and action capabilities to approved clients.

Never expose database access, storage credentials or unrestricted vendor tools through MCP.
