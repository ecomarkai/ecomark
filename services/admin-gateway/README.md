# Admin gateway

Backend-for-frontend for Control Center. It aggregates read models and routes commands to owning
services. It owns no business data and never writes another service database directly.
